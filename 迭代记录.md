# 迭代记录

## 2026-03-11
- 新增 Skills 榜单跟踪器项目骨架（`index.html` / `styles.css` / `app.js`）。
- 新增数据构建脚本 `scripts/fetch-and-build.js`：
  - 接入 `skills.sh` 官方接口 `api/skills/{all-time|trending|hot}/0`。
  - 每榜取前100并合并去重。
  - 拉取 skill 详情页文本并抽取用途。
  - 推断适合人群、适合场景。
  - 计算综合热度 `totalHeat`。
  - 输出 `data/skills-current.json`、`data/skills-history.json`，并同步生成本地直开用 `*.js`。
- 实现“掉榜区”机制：历史入榜技能在掉出前100后进入折叠区展示。
- 新增 GitHub Actions 工作流 `update-skills-tracker.yml`：
  - 每日自动更新数据
  - 自动提交数据变更
  - 自动发布到 GitHub Pages

## 下一步
- 增加卡片展开态（展示更完整 SKILL.md 摘要）。
- 增加厂商维度聚合分析（厂商占榜、热度趋势）。
- 增加“新上榜/连续上榜/掉榜”状态徽章。
